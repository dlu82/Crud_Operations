import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    width: 50,
    height: 50,
    backgroundColor: '#9ba2a3',
    position: 'absolute',
    bottom: 50,
    right: 50,
    borderRadius: 25,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
export default style;
